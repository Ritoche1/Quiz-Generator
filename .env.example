# =====================================================
# 🌟 Quiz Generator Environment Configuration
# =====================================================
# Copy this file to .env and update the values below
# Never commit your actual .env file to version control!

# 🤖 AI Configuration (Required)
# Get your API key from: https://console.mistral.ai/
MISTRAL_API_KEY=your_mistral_api_key_here

# 🌐 Backend Server Configuration  
BASE_URL=http://localhost:5000
ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# 🎨 Frontend Configuration (Required for Next.js)
# This URL is used by the frontend to connect to the backend API
NEXT_PUBLIC_BASE_URL=http://localhost:5000

# 🐘 Database Configuration
DATABASE_URL=postgresql+asyncpg://quizuser:quizpass@postgres:5432/quizdb

# 📊 Application Configuration
VERSION=1.0.0

# =====================================================
# 🚀 Production Configuration (Optional)
# =====================================================
# Uncomment and modify these for production deployment:

# Production Backend
# BASE_URL=https://your-domain.com/api
# JWT_SECRET=your_production_jwt_secret_super_secure_long_string

# Production Frontend 
# NEXT_PUBLIC_BASE_URL=https://your-domain.com/api

# Production Database
# DATABASE_URL=postgresql+asyncpg://prod_user:prod_pass@prod_host:5432/prod_db

# =====================================================
# 🔧 Development Notes
# =====================================================
# 
# For Docker development (recommended):
# - Frontend runs on: http://localhost:83
# - Backend runs on: http://localhost:5000  
# - Database runs internally in Docker network
#
# For local development without Docker:
# - Frontend runs on: http://localhost:3000 (npm run dev)
# - Backend runs on: http://localhost:5000 (uvicorn main:app --reload)
# - Database: Set up PostgreSQL locally or use Docker for DB only
#
# Environment Variables Explanation:
# - MISTRAL_API_KEY: Your Mistral AI API key for quiz generation
# - BASE_URL: Backend API base URL (used by backend for internal config)
# - NEXT_PUBLIC_BASE_URL: Frontend's connection to backend (publicly accessible)
# - JWT_SECRET: Secret key for JWT token signing (keep this secure!)
# - DATABASE_URL: PostgreSQL connection string
# - VERSION: Application version (displayed in footer)
#
