# =====================================================
# 🎨 Quiz Generator Frontend Environment Configuration  
# =====================================================
# This file is specific to the Next.js frontend application
# Copy this to .env.local in the same directory

# 🌐 Backend API Connection (Required)
# URL where the backend API is running
NEXT_PUBLIC_BASE_URL=http://localhost:5000/api

# 📊 Application Version (Optional)
# This is displayed in the footer
VERSION=1.0.0

# =====================================================
# 🚀 Deployment Configurations
# =====================================================

# For Docker development (default):
# NEXT_PUBLIC_BASE_URL=http://localhost:5000/api

# For production deployment:
# NEXT_PUBLIC_BASE_URL=https://your-domain.com/api

# For local development (backend on different port):
# NEXT_PUBLIC_BASE_URL=http://localhost:5000/api

# =====================================================
# 📝 Environment Notes
# =====================================================
# 
# NEXT_PUBLIC_ variables are exposed to the browser!
# - Never put secrets in NEXT_PUBLIC_ variables
# - These are visible in the client-side JavaScript
# - Only use for non-sensitive configuration
#
# This frontend needs the backend API to be running at
# the URL specified in NEXT_PUBLIC_BASE_URL
#