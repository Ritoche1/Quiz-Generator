# =====================================================
# ⚡ Quiz Generator Backend Environment Configuration
# =====================================================  
# This file is specific to the FastAPI backend application
# Copy this to .env in the same directory

# 🤖 AI Configuration (Required)
# Get your API key from: https://console.mistral.ai/
MISTRAL_API_KEY=your_mistral_api_key_here

# 🌐 Server Configuration  
BASE_URL=http://localhost:5000
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 🔐 Security (Required)
# Use a strong, random secret key for JWT signing
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# 🐘 Database Configuration
DATABASE_URL=postgresql+asyncpg://quizuser:quizpass@postgres:5432/quizdb

# =====================================================
# 🚀 Production Configuration Examples
# =====================================================

# Production database (uncomment and modify):
# DATABASE_URL=postgresql+asyncpg://prod_user:prod_pass@prod_host:5432/prod_db

# Production JWT secret (use a long, random string):
# JWT_SECRET=your_very_long_and_secure_production_jwt_secret_key_here

# Production base URL:
# BASE_URL=https://your-api-domain.com

# =====================================================
# 🔧 Development Notes
# =====================================================
#
# For Docker development:
# - Database runs in Docker container
# - DATABASE_URL should point to 'postgres' hostname
#
# For local development:  
# - Set up local PostgreSQL or use Docker for DB only
# - DATABASE_URL should point to 'localhost'
#
# Security reminders:
# - Never commit real secrets to version control
# - Use different JWT_SECRET for each environment
# - Keep MISTRAL_API_KEY private and secure
#
